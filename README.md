# to-do-app
A clean and responsive To-Do App built with HTML, CSS, and React JS. Features include adding, editing, deleting tasks, marking them complete, filtering (All/Active/Completed), progress tracking, and saving tasks in local Storage for persistence.
